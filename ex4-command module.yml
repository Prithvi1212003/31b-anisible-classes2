-
 name: usecase on command module
 hosts: client1
 tasks:
  - name: below code to create a directory
    command:
     cmd: mkdir /opt/Uk

  - name: below to remove git application
    command:
     cmd: yum remove -y git